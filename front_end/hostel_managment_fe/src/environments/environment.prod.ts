export const environment = {
  production: true,
  // Set this to your deployed backend URL (e.g. https://hostel-ms-api.onrender.com)
  apiUrl: (window as any).__API_URL__ || 'https://hostel-ms-api.onrender.com',
};
